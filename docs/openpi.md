# openpi pipeline

Closed-loop interpretability pipeline for the π₀.₅ backbone (PaliGemma
vision-language prefix + action expert).

Migration in progress. The same four-stage pipeline as
[`openvla.md`](openvla.md) will be exposed here.
